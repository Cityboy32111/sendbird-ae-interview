# Scoring Methodology

**Status: Phase 8 deliverable — not produced in Phase 0.**

The scoring *design* (all weights, sub-score inputs, normalization, confidence
floor) is fully specified in `config/scoring_config.json`. This document will
narrate that methodology with worked examples and per-score rationale once the
scoring pass runs in Phase 8. See `docs/buyer_strategy_alignment.md` for how the
Amplitude-fit scores map to Amplitude product surfaces.
