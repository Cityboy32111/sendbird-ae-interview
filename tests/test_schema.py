"""External Product Intelligence Graph — tests/test_schema.py

Validate that every stored data point carries company_id, field_name, value, source_name, source_url, extraction_date, confidence, evidence_id, inference_flag.

NOT IMPLEMENTED until Phase 0 is approved.
"""

import pytest


@pytest.mark.skip(reason="Implemented after Phase 0 approval")
def test_placeholder() -> None:
    raise NotImplementedError
