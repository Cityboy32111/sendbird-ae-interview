"""External Product Intelligence Graph — detect_technographics.py

Phase 3: Detect analytics/product tools from HTML, script tags, tag manager, privacy-policy vendors, docs SDK mentions. BuiltWith fallback if code detection is weak.

NOT IMPLEMENTED. No scraping, enrichment, or DB writes occur until Phase 0 is
approved in writing. See docs/runbook.md for execution order.
"""


def main() -> None:
    raise NotImplementedError(
        "Phase 3: implement after Phase 0 approval."
    )


if __name__ == "__main__":
    main()
